<!DOCTYPE html>
<html>
  <head>
    <title>MiniCapstoneClient</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <nav class="topnav">
      <a href="/">Home</a>
      <a href="/client/products/">All coffees</a>
      <a href="/client/products/new">Add a coffee</a>
      <a href="#about">About</a>
      <a href="/signup">Sign Up</a>
      <a href="/client/login">Log In</a>
      <a href="/client/logout" data-method="delete">Log Out</a>
      <a href="/client/carted_products">View Cart</a>
    </nav>
    <nav class="topnav">
      <a href="/client/products/?category=lightblend">Light Blend</a>
      <a href="/client/products/?category=mediumblend">Medium Blend</a>
      <a href="/client/products/?category=darkblend">Dark Blend</a>
      <a href="/client/products/?category=blackasmysoulblend">Black as my Soul Blend</a>
    </nav>
    <% if @jwt %>
      <p class="flash-hash flash-jwt"><%= @jwt %></p>
    <% end %>
    <% flash.each do |key, message| %>
      <% if key != 'success' %>
        <p class="flash-hash flash-danger "><%= message %><p>
      <% else %>
        <p class="flash-hash flash-success"><%= message %><p>
      <% end %>
    <% end %>

    <%= yield %>
  </body>
</html>
